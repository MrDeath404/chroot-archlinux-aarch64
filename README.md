```sh
bash <(curl -s https://raw.githubusercontent.com/MrDeath404/chroot-archlinux-aarch64/main/install.sh)
```
or
```sh
git clone https://github.com/MrDeath404/chroot-archlinux-aarch64.git && cd chroot-archlinux-aarch64 && chmod +x ./install.sh && ./install.sh
```